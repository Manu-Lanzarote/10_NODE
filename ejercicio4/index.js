// 4.	Escribe un módulo propio con una función que recibe por parámetro dos números y muestra por consola el menor de los dos. Impórtalo en un archivo index.js y muestra el resultado.

const funcionComparar = require("./comparar");
const comparar = require("./comparar");
console.log(funcionComparar(67, 200));
